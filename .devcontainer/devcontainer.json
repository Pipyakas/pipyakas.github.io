{
  "name": "Jekyll Development",
  "image": "mcr.microsoft.com/devcontainers/jekyll:latest",
  "forwardPorts": [4000],
  "postCreateCommand": "bundle install --quiet && sudo apt-get update && sudo apt-get install -y --no-install-recommends texlive-latex-base texlive-latex-extra texlive-fonts-recommended texlive-fonts-extra latexmk python3",
  "postStartCommand": "bundle exec jekyll serve --host 0.0.0.0 --port 4000 --livereload",
  "customizations": {
    "vscode": {
      "extensions": ["runemadsen.readme-vscode"]
    }
  }
}
