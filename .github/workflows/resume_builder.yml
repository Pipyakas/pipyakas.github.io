name: Build and Deploy Resume

on:
  push:
    paths:
      - 'resume/resume.json'
      - 'resume/resume_vi.json'
      - 'resume/resume.tex.template'
      - 'scripts/generate_tex.py'
      - '.github/workflows/resume_builder.yml'

jobs:
  build_latex:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Generate LaTeX from JSON
        run: python3 scripts/generate_tex.py --locale all

      - name: Compile LaTeX to PDF (EN)
        uses: xu-cheng/latex-action@v3
        with:
          root_file: resume/resume.tex
          latexmk_use_xelatex: true

      - name: Compile LaTeX to PDF (VI)
        uses: xu-cheng/latex-action@v3
        with:
          root_file: resume/resume_vi.tex
          latexmk_use_xelatex: true

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Deploy PDFs to assets
        run: |
          mkdir -p assets
          cp resume/resume.pdf assets/quy.nguyenngoc.pdf
          cp resume/resume_vi.pdf assets/quy.nguyenngoc-vi.pdf
          git add assets/quy.nguyenngoc.pdf assets/quy.nguyenngoc-vi.pdf
          git diff --staged --quiet || git commit -m "Update resume PDFs [skip ci]"
          git push
