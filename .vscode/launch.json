{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Preview Site (Chrome)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:4001",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Jekyll: Start Preview"
    }
  ]
}
